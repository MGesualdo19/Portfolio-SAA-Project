[pytest]
testpaths = tests
markers =
    network: needs live Yahoo Finance data (deselect with -m "not network")
    slow: takes more than ~20s (deselect with -m "not slow")
filterwarnings =
    ignore::DeprecationWarning
    ignore::FutureWarning
